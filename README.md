# JDIH Kabupaten Lamongan — Backend

## Tech Stack

- NestJS + TypeScript + MySQL (TypeORM + raw query)

## Getting Started

1. Node v20 (nvm), pnpm
2. Salin `.env.example` → `.env`
3. Buat database `db_jdih_lmg`
4. Migration DB `pnpm run migration:run`
